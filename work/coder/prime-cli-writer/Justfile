lint:
    uv run ruff check .
    uv run ruff format --check .

typecheck:
    uv run mypy --strict src tests

test:
    uv run pytest --tb=short --strict-markers -v

check:
    just lint
    just typecheck
    just test

fix:
    uv run ruff format .
    uv run ruff check --fix .
